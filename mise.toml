[tools]
go = "latest"
golangci-lint = "latest"
gotestsum = "latest"

[tasks.lint]
run = "go fmt ./... && golangci-lint run --fix ./..."

[tasks.test]
run = "gotestsum"

[tasks.tidy]
run = """
go get -u ./...
go mod tidy
"""
